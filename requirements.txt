# Core inference
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.29.0          # HuggingFace multi-GPU utilities
sentencepiece>=0.1.99       # Llama tokenizer dependency
protobuf>=3.20.0            # Required by sentencepiece

# Server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0   # ASGI server with websocket + http/2 support
pydantic>=2.0.0

# Queue
redis>=5.0.0                # async Redis client (redis.asyncio)

# Metrics
prometheus-client>=0.20.0

# Benchmarking
httpx>=0.27.0               # async HTTP client for load testing
numpy>=1.26.0               # percentile calculations
